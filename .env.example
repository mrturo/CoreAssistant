# Example .env file for Todoist integration
# Copy this file as .env and update the values

# Time zone configuration (optional, uses system default if not specified)
TIME_ZONE=America/Mexico_City

# Ignored lists (optional, comma-separated list IDs)
IGNORED_LISTS=

# Todoist API token (required for Todoist integration)
# Get your token from: https://todoist.com/prefs/integrations
# Go to "Settings" -> "Integrations" -> "API token"
TODOIST_API_TOKEN=your_todoist_api_token_here

# Google OAuth configuration (existing)
GOOGLE_OAUTH_CLIENT_SECRETS=config/gcp/credentials.json
GOOGLE_OAUTH_TOKEN=config/gcp/token.json
GOOGLE_OAUTH_CONSOLE=
GOOGLE_OAUTH_PORT=0